
body {
    margin: 0;
    padding: 0;
}

img {
    /* Define the initial properties for the image */
    transition: property duration timing-function delay;
  }
  
  /* This property is used to change the properties of the image on hover */
   img:hover {
    /* Define the properties to apply when hovering over the image */
    transform: transform-functions; /* You should replace 'transform-functions' with actual values */
  }
  .div-img{
    justify-content: center;
    align-items: center;
    display:flex;
    padding-top:20%;
}
img{
    transition: transform .2s;
    width:50px;
    height:50px;
    margin:0 auto;
    background-color: rgb(173, 173, 237);
    border-radius: 10px;
    border: 1px solid black;
}
img:hover{
    transform:scale(2.5);
}